const Area = () => { 
    return(
        <div style={tap}>
            <h2>Area Page</h2>
        </div>
    )
}

export default Area

const tap = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    height:'100'
}